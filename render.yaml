services:
  - type: web
    name: calendar-booking
    runtime: docker
    dockerfilePath: ./Dockerfile
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        generateValue: false
        value: 3000
    healthCheckPath: /event-types
    autoDeploy: true
